# PostgreSQL 연결 (Railway에서 제공하는 URL 형식)
DATABASE_URL=postgresql://user:password@host:port/dbname

# 서버 포트 (Railway는 자동으로 PORT 환경변수를 주입함)
PORT=3000

# 환경 (development | production)
NODE_ENV=development
